export default function Terms() {
  return (
    <div className="container" style={{ paddingBlock: 'var(--space-10)' }}>
      <h1 className="font-display" style={{ fontSize: 'var(--text-2xl)' }}>Terms of service</h1>
      <p style={{ color: 'var(--color-ink-soft)', marginTop: 'var(--space-2)' }}>
        Page structure only — built out in a later phase.
      </p>
    </div>
  )
}
